{
  "name": "cash-frontend",
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream",
    "format": "lerna run format --stream",
    "prebuild": "rm -rf deploy && mkdir deploy deploy/auth deploy/work-time-calendar deploy/auth-helper",
    "build": "lerna run build",
    "postbuild": "cp packages/root-config/dist/* deploy && cp packages/auth/dist/* deploy/auth && cp packages/work-time-calendar/dist/* deploy/work-time-calendar && cp packages/auth-helper/dist/* deploy/auth-helper ",
    "deploy": "yarn bootstrap && yarn build",
    "preserve": "yarn build",
    "serve": "npx serve deploy -s -n -C",
    "postinstall": "lerna bootstrap"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
